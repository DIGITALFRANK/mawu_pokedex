
const pokemon_container = {
    all: () => {
        console.log("what's returned from the pokemon API, 3 pokemon objects in an array format")
        return // the array
    },
    get: (pokemon) => {
        return // pokemon object called by its name
    }
}